{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "sessions": {
      "$sessionId": {
        "$userId": {
          ".read": "auth != null",
          ".write": "auth != null"
        }
      }
    }
  }
}

